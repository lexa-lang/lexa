def fib(n) {
    val cond = n == 0; 
    if cond then 0 else
        val cond = n == 1;
        if cond then 1 else
            val a = n - 1;
            val b = n - 2;
            val v1 = fib(a);
            val v2 = fib(b);
            v1 + v2
}

def main() {
    val arg = ~readInt();
    val res = fib(arg);
    val _ = ~printInt(res);
    0
}