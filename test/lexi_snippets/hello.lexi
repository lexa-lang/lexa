effect Hello {
    dcl hello
}

def run(n) {
    val s = newref {42};
    handle {
        raise hello_stub.hello 
    } with hello_stub: Hello {
        def hello(_) { 
            val _ = ~printChar(104);
            val _ = ~printChar(101);
            val _ = ~printChar(108);
            val _ = ~printChar(108);
            val _ = ~printChar(111);
            val _ = ~printChar(44);
            val _ = ~printChar(32);
            val _ = ~printInt(s[0]);
            0
        }        
    }
}

def main() {
    run(~readInt())
}
